{
  "name": "posgres",
  "version": "1.0.0",
  "description": "local postgres",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:postgres": "docker-compose -f compose.yml up",
    "start:postgres-ui": "open-cli http://localhost:8082",
    "start:dev": "run-p start:postgres start:postgres-ui"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "open-cli": "^6.0.1"
  }
}
